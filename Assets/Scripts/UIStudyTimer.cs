using UnityEngine;

public class UIStudyTimer : UIButton
{
    // 버튼 클릭 -> 버튼 멈춤 -> 그에 대한 걸 처리 하고 -> 반응해서 패킷으로 값이 올라가는
    // UI에서 하는 것
    // 데이터를 처리하는 것
    // 그리고 처리한 데이터로 패킷을 보내는 것 -> 이게 패킷 감인가? -> 시간 변화
}